## .NET build output
bin/
obj/
publish/

## Rider / VS
.idea/
.vs/
*.user

## Claude Code local (machine-specific) settings — keep .mcp.json, ignore local overrides
.claude/settings.local.json

## Local SQLite scratch (BeSQL persists inside the browser, not to disk here)
*.sqlite3
*.db
